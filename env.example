# CoreIQ Scorer — .env.example
# Copy this file to `.env.local` and fill with real values.
# Never commit `.env.local` to Git; keep secrets out of the repo.

# Public (safe for client-side use)
NEXT_PUBLIC_SUPABASE_URL=https://YOUR-PROJECT-REF.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=YOUR-SUPABASE-ANON-KEY

# Optional (server-only, for future server routes/actions). 
# Do NOT prefix with NEXT_PUBLIC_ and do NOT expose on the client.
# Set this only in Vercel’s Environment Variables, not locally unless you run server code.
SUPABASE_SERVICE_ROLE_KEY=
